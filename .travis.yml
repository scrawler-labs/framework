language: php
php:
 - '7.2'
 - '7.3'
 - '7.4'

branches:
  only:
    - master


before_script:
  - sudo apt-get update
  - composer install --dev


script: phpunit --configuration phpunit.xml --coverage-clover ./tests/logs/clover.xml

after_script:
  - php vendor/bin/coveralls -v